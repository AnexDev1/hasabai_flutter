name: hasab_ai_flutter_example
description: "Demo app for Hasab AI Flutter SDK"
publish_to: 'none' # Prevent accidental publishing

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  hasab_ai_flutter:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true